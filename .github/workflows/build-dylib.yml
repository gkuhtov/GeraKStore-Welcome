name: Build WelcomeToSpace dylib

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - "WelcomeToSpace/**"
      - ".github/workflows/build-dylib.yml"

jobs:
  build:
    runs-on: macos-26

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Verify WelcomeToSpace resources
        run: |
          echo "Starting WelcomeToSpace build..."
